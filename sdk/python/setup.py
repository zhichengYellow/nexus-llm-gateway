from setuptools import setup, find_packages

setup(
    name="nexus-sdk",
    version="0.1.0",
    description="Nexus LLM Gateway Python SDK",
    packages=find_packages(),
    python_requires=">=3.8",
)
